<div class="menu" align="center">
	<h3>Seasons</h3>
	<nav>
		<% @seasons.each do |season| %>
			<li><%= link_to(season.year, :controller => 'statistics', :action => 'result', :id => season.id) %></li>
		<% end %>
	</nav>
</div>